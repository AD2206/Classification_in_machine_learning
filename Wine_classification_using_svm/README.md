We have divided wines into three categories:0,1,2.This machine learning model is used to predict whether the wine belongs to the categories 0,1,2.It uses support vector machine(svm).
This model has reported an accuracy score of 84 percent.
